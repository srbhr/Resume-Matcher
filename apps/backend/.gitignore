# Database files (local data)
data/*.json
data/config.json
# TinyDB dump left behind by the SQLite migration — contains real resume PII.
# `data/*.json` does not match it because the name ends in `.migrated`.
data/*.migrated
data/database.json*
!data/.gitkeep

# Config file with API keys - NEVER COMMIT
config.json

# Environment
.env

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
*.egg-info/
dist/
build/

# IDE
.idea/
.vscode/
*.swp
